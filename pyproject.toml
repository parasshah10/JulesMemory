[project]
name = "jules"
version = "0.1.0"
description = "MCP server for Jules — memory and research capabilities"
dependencies = [
    "fastmcp",
    "requests",
    "openai",
]

[project.scripts]
jules = "server:main"
