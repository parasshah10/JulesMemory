[project]
name = "jules-memory"
version = "0.1.0"
description = "MCP wrapper for Jules' Hindsight memory bank"
dependencies = [
    "fastmcp",
    "requests",
]

[project.scripts]
jules-memory = "server:main"
